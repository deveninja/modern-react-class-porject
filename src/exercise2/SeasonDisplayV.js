import React from 'react'

const SeasonDisplayV = (props) => {
  return (
    <div>
      <h4>Latitude: {props.lat}</h4>
      <h4>Longitude: {props.long}</h4>
    </div>
  )
}

export default SeasonDisplayV
